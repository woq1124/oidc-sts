/**
 * @typedef {{
 * issuerUrl: string;
 * clientId: string;
 * clientSecret?: string;
 * redirectUri: string;
 * roleArn: string;
 * roleSessionName?: string;
 * duration: number;
 * scope?: string;
 * force?: boolean;
 * clean?: boolean;
 * }} Args
 */

export default {};
